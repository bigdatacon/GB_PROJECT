Скрипт по работе модели LR по прогнозированию стоимости домов.
Модель строится для Spark и хранится в HDFS (файл TRAIN), данные для прогноза приходят из kafka в json (Файл STREAM) далее обрабатываются и выдают результат


